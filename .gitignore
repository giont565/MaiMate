# 官方命題資料：僅供競賽使用、勿轉傳（主辦規定）——原始檔只放 Drive
data/MaiCoin_transactions.csv
data/MaiCoin_*.csv
data/*.zip

# 金鑰與本地設定
.env
*.pem
samconfig.toml

# 本地快照與文件成品不進版控（放 Drive；程式碼走分支＋PR——workflow.md git 紀律）
/*.zip
/*.docx

# 建置產物（docs 簡報產生器的暫用 npm 檔勿入 git；根目錄 package.json 是煙測用、要進版控）
# ⚠️ 不要加斜線：`node_modules/` 只擋目錄，擋不住同名的**符號連結**。
# 2026-08-02 就是這樣被繞過的——worktree 裡為了跑 smoke 建的
# `node_modules -> /Users/hao/Documents/MaiMate/node_modules` 被 commit 進 de66bd2(#53)，
# 主目錄 checkout 後變成指向自己的死循環，playwright 報 ELOOP、前端測試全掛。
node_modules
docs/**/package.json
docs/**/package-lock.json
__pycache__/
*.pyc
.aws-sam/
docs/*.pdf
docs/slide-*.jpg
.DS_Store
smoke_*.png

# 評估用打包（內容都在 docs/ 裡，不需要版控壓縮檔）
docs/MaiMate_review-bundle.zip
docs/review-bundle/03_PITCH_DECK.pdf
docs/PITCH_DECK.standalone.html

# 影片素材：Demo 錄影產出（mp4/webm/frames）不進 git——單支就好幾 MB，且每次重錄都變。
# 放 Drive「黑客松/MaiMate_決賽影片」，repo 只留產生它們的腳本（scripts/record_demo.js 等）。
*.mp4
*.webm
*_frames/
*.mov
# 例外：入口動畫 01–03 幕的實拍是產品素材，不是錄影產出——播放器要載、Service Worker
# 要 precache，缺了動畫會靜悄悄退回 CSS 版前三幕（不報錯，所以很難發現）。它已經在
# 版控裡，這行是把「刻意留著」寫清楚，免得後人當成漏擋的順手刪掉。
!frontend/brand/intro-shot1-3.mp4
# 例外二（08-02）：PITCH_DECK.html 第 4–8 頁的五個影片插槽吃 docs/demo/0X_*.mp4。
# 這五支不進版控，簡報在別人機器上會**靜靜地**退回靜態截圖——播放邏輯是「載得到才換成
# 影片，載不到就留著 pitch_shots/*.png」，不報錯，所以沒人會發現自己在放假的簡報。
# 08-02 就是這樣壞了一整天：HTML 指的五個檔名（01_profile…05_execute）從來沒存在過，
# docs/demo/ 裡躺的是另外五個名字（01_entry…05_health）。這行讓插槽跟著 repo 走。
# 範圍刻意收窄成 01–05：只有簡報真的吃的那五格進版控。其餘錄影產出（完整動線、
# 四頁籤巡覽、入口動畫等）維持只留 Drive，免得 repo 被每次重錄的素材撐大。
!docs/demo/0[1-5]_*.mp4
# 兩套劇本的備援：第 5–8 頁有 ETH 全賣與 USDT 買入兩版（簡報標題目前是 ETH，
# docs/DEMO_SCRIPT.md 主線是 USDT，兩邊尚未對齊，所以都留著）。
# 用 docs/demo/use_scenario.sh eth|usdt 切換——它換的是檔案不是 HTML。
!docs/demo/scenarios/*/0[1-5]_*.mp4

# 隊內文件不進公開 repo（repo 依題目要求已 PUBLIC，主辦與評審點得進來）：
# 評審攻防教戰、風險盤點、資料檢定圖表——這些是給自己人看的，寫法不適合對外。
# 放 Drive「黑客松/MaiMate_隊內」，repo 只留產生它們的程式。
docs/_internal/
